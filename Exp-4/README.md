# Experiment 4 — Learning Outcomes 


1. **Local State** — `useState` — `count` — `Increase` / `Decrease` buttons
2. **Context API** — `createContext` — `CounterContextProvider` — `useContext` — `count`
3. **Redux** — `configureStore` — `counterSlice` — `increment` / `decrement` / `setCount`
4. **Components** — `CounterLocalState`, `CounterGlobalContextParent`, `CounterGlobalReduxParent` — `App.css` `.box`
5. **main.jsx** — `Provider(store)` — `App` — Redux `count`
6. **index.css** — gradient background — `prefers-color-scheme` dark / light
7. **Hover / Interaction** — `.box:hover` transform / scale / shadow — `.box-btn:hover` transform / shadow / transition

